[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
pythonpath = src
markers =
    real_llm: tests that call a real local or remote LLM provider
    desktop_mcp: tests that start the Desktop MCP server or access desktop resources
    feishu_live: tests that send real messages through Feishu
